openapi: 3.1.0
info:
  title: be-py-eco API
  version: 1.0.0
  description: E-commerce backend API for be-py-eco.
paths:
  /api/v1/storefront/accounts/login:
    post:
      operationId: storefront_accounts_login_create
      tags:
      - storefront
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Login'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/Login'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/Login'
        required: true
      security:
      - {}
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TokenResponse'
          description: ''
  /api/v1/storefront/accounts/me:
    get:
      operationId: storefront_accounts_me_retrieve
      tags:
      - storefront
      security:
      - jwtAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CustomerResponse'
          description: ''
  /api/v1/storefront/accounts/register:
    post:
      operationId: storefront_accounts_register_create
      tags:
      - storefront
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Register'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/Register'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/Register'
        required: true
      security:
      - {}
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CustomerResponse'
          description: ''
  /api/v1/storefront/accounts/sessions:
    get:
      operationId: storefront_accounts_sessions_list
      tags:
      - storefront
      security:
      - jwtAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/SessionResponse'
          description: ''
  /api/v1/storefront/accounts/sessions/{session_id}/revoke:
    post:
      operationId: storefront_accounts_sessions_revoke_create
      parameters:
      - in: path
        name: session_id
        schema:
          type: string
          format: uuid
        required: true
      tags:
      - storefront
      security:
      - jwtAuth: []
      responses:
        '204':
          description: No response body
  /api/v1/storefront/accounts/sessions/revoke-all:
    post:
      operationId: storefront_accounts_sessions_revoke_all_create
      tags:
      - storefront
      security:
      - jwtAuth: []
      responses:
        '204':
          description: No response body
  /api/v1/storefront/accounts/token/refresh:
    post:
      operationId: storefront_accounts_token_refresh_create
      tags:
      - storefront
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RefreshToken'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/RefreshToken'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/RefreshToken'
        required: true
      security:
      - {}
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TokenResponse'
          description: ''
  /api/v1/storefront/accounts/verify-email:
    post:
      operationId: storefront_accounts_verify_email_create
      tags:
      - storefront
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/VerifyEmail'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/VerifyEmail'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/VerifyEmail'
        required: true
      security:
      - {}
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CustomerResponse'
          description: ''
components:
  schemas:
    CustomerResponse:
      type: object
      description: Response shape only — used for OpenAPI docs, not for validation.
      properties:
        id:
          type: string
          format: uuid
        email:
          type: string
          format: email
        email_verified:
          type: boolean
      required:
      - email
      - email_verified
      - id
    Login:
      type: object
      properties:
        email:
          type: string
          format: email
        password:
          type: string
          writeOnly: true
      required:
      - email
      - password
    RefreshToken:
      type: object
      properties:
        refresh:
          type: string
      required:
      - refresh
    Register:
      type: object
      properties:
        email:
          type: string
          format: email
        password:
          type: string
          writeOnly: true
      required:
      - email
      - password
    SessionResponse:
      type: object
      description: Response shape only — used for OpenAPI docs, not for validation.
      properties:
        id:
          type: string
          format: uuid
        created_at:
          type: string
          format: date-time
        expires_at:
          type: string
          format: date-time
      required:
      - created_at
      - expires_at
      - id
    TokenResponse:
      type: object
      description: Response shape only — used for OpenAPI docs, not for validation.
      properties:
        access:
          type: string
        refresh:
          type: string
      required:
      - access
      - refresh
    VerifyEmail:
      type: object
      properties:
        token:
          type: string
      required:
      - token
  securitySchemes:
    jwtAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
